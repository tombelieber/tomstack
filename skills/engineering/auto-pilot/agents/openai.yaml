interface:
  display_name: "Auto Pilot"
  short_description: "Deliver a verified PR and optional fresh release"
  default_prompt: "Use $auto-pilot in pr mode for this approved plan; use ship only when I explicitly authorize production release."
policy:
  allow_implicit_invocation: false
